<div class="container">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2">
      <h1 class="text-center">Articles</h1>
      <br>
      <h3><%= link_to "créer un nouvel article", new_article_path %></h3>
      <br>
      <% @articles.each do |article| %>
        <h3><%= link_to article.title, article_path(article) %></h3>
        <p><%= article.content %></p>
      <% end %>
    </div>
  </div>
</div>

